---
- hosts: vps
  tasks:

    - name: "Copy Caddyfile"
      copy:
        src: "../resources/Caddyfile"
        dest: "/etc/caddy/Caddyfile"
        mode: 0644

    - name: "Create serve directory"
      file:
        path: "{{ vps.serve.root_path }}"
        state: directory
        mode: 0755
        owner: "{{ vps.users.unprivileged.name }}"
        group: "{{ vps.users.unprivileged.name }}"
